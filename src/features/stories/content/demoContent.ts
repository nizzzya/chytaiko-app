import type { Story, StoryPage } from '../../../types/story';

import { mockStories, mockStoryPages } from '../data/mockStories';

// TODO: Replace with final demo content later (content phase).
// TODO: Keep test content only until content phase.
// TODO: Validate with storyValidationService before release.

/** Demo catalog stories — currently mirrors mock test data. */
export const demoStories: Story[] = mockStories;

/** Demo story pages — currently mirrors mock test data. */
export const demoStoryPages: StoryPage[] = mockStoryPages;
