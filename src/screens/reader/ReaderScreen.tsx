import { ScreenPlaceholder } from '../shared/ScreenPlaceholder';

export function ReaderScreen() {
  return (
    <ScreenPlaceholder
      title="Читання"
      subtitle="Режим читання — не підключено"
    />
  );
}
