import { ScreenPlaceholder } from '../shared/ScreenPlaceholder';

export function ProfileScreen() {
  return (
    <ScreenPlaceholder
      title="Профіль"
      subtitle="Налаштування — незабаром"
    />
  );
}
