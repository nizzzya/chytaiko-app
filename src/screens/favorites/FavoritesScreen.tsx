import { ScreenPlaceholder } from '../shared/ScreenPlaceholder';

export function FavoritesScreen() {
  return (
    <ScreenPlaceholder
      title="Обране"
      subtitle="Обрані казки — незабаром"
    />
  );
}
